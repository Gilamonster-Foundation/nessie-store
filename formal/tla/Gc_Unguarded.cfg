\* Boundary model: the in-flight write-guard is OFF. This model is EXPECTED TO
\* FAIL — TLC must report a violation of InflightProtected, exhibiting the put→
\* reference race: b1 is Put (stored + in-flight) and, before it is Referenced, a
\* Gc pass sweeps it (it is not yet a root), leaving it in-flight but not stored.
\* That counterexample proves the write-guard is load-bearing, not decorative.
SPECIFICATION Spec

CONSTANTS
    Blobs = {b1, b2}
    GuardInflight = FALSE

INVARIANT TypeOK
INVARIANT InflightProtected
